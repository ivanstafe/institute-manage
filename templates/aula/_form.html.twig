{% block stylesheets %}
    <link href="{{asset('css/nueva.css')}}" type="text/css" rel="stylesheet">
    
{% endblock %}
{% block body %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <br>
    <button class="custom-button">{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}
{% endblock %}
